# Frontier Reframe — Binding Tax, Privacy Ladder, and Next Steps

*Synthesis of the 2026-05-29 reframing discussion. Captures the conceptual
findings, the reframed thesis spine, the per-implementation privacy
classification, the open choices, and the concrete next steps. Numbers are from
the committed `results/` CSVs (anchor N=480, flat baseline N=500).*

---

## Part 1 — The findings (what we worked out)

### F1. The recursive implementation hides everything as witness
The outer circuit takes the K inner proofs **and their public inputs** as private
witness, verifies them in-circuit (`verify_honk_proof`), and re-runs the glue logic
against those now-trusted-but-private values. Only `(root, threshold)` are public —
the same surface as flat_merkle. So recursion's public surface = perfect/structural
hiding. (`circuits/recursion/src/main.nr`.)

### F2. The external Python verifiers are a minimal, sound binding layer
`verify_hier.py` / `verify_hier_fs.py` run `bb verify` ×(K+1) then do **pure
Field-equality cross-checks** (no field arithmetic) binding the proofs to one
instance. `bb verify` alone only proves each proof internally consistent — nothing
binds K+1 independent proofs to the *same* instance. Justification: the checks are
deterministic, public, assumption-free integer comparisons on values `bb verify` has
already cryptographically bound; they expand the *trusted code* surface, not the
*cryptographic* trust base; they faithfully mirror the in-circuit asserts recursion
uses. This is conventional for aggregation/PCD/rollup verifiers.

### F3. The stitching tax — one artifact, three symptoms
Decomposing into K independent segment-proofs forces you to bind them back into one
statement. That binding is **a single object with three coupled symptoms**, all of
which are the *shared public surface of independent proofs*:
1. **partition leakage** (the shared values are disclosed),
2. **O(K) verifier cost** (K+1 proofs → K× proof bytes + K× verify time),
3. **verifier-side bookkeeping** (the equality cross-checks).

They dissolve **together** under one move: fold the binding into a proof. Recursion
does this (the asserts become in-circuit; verifier returns to one `bb verify`).
Folding does it cheaply.

### F4. The pick-two triangle
At fixed privacy, three desiderata; each architecture gives exactly two:

| | Parallel + low per-prover mem (**P**) | O(1) verifier (**V**) | Low prover overhead (**C**) |
|---|:--:|:--:|:--:|
| flat (K=1) | ✗ | ✓ | ✓ |
| plain-sort / plain-product / committed-* | ✓ | ✗ | ✓ |
| recursion | ✓ | ✓ | ✗ |
| **folding** (future) | ✓ | ✓ | ✓ (breaks the triangle) |

The O(K) verifier cost is **the price plain-sort/plain-product pay to keep P and C together** — a
defining feature of the corner, not a defect.

### F5. The family is generated by two binding decisions
- **Where binding lives:** verifier-side (plain-sort, plain-product, B) / in-circuit (recursion) /
  deferred (folding).
- **What is bound:** plaintext (discloses — the leak; good when disclosure is the
  goal) / hiding commitment (committed-sort/plain-product) / witness (recursion — the limit).

### F6. The commitment fix closes the leak without recursion
Bind on **hiding commitments** instead of plaintext: each sub exposes
`C_i = Commit(values; r_i)`, the glue takes the values+openings as witness and does
the partition/boundary/cost checks in-circuit, the verifier checks `sub_i.C_i ==
glue.C_i`. Public surface collapses to `(root, threshold, C_0..C_{K-1})`. The
bookkeeping becomes ZK automatically (comparing opaque blobs). Can be collapsed to one
digest per proof → O(K) single-field checks.

### F7. plain-sort is NOT dominated by plain-product (the key correction)
Glue-to-glue, plain-product's grand product beats plain-sort's sort (N=480 K=2: 6,987 gates / 31 MB vs
14,822 / 159 MB). **But plain-product relocates that O(N) work into the K segments** (+~3.5% per
sub), so on totals plain-sort is *cheaper at every measured K*:

| Total gates (N=480) | K=2 | K=4 | K=8 |
|---|---|---|---|
| plain-sort | 769,926 | 775,622 | 787,014 |
| plain-product | 788,533 | 794,401 | 806,137 |

Per-prover memory **ties at K=8** (~159 MB) for opposite reasons (plain-sort pinned by O(N)
glue floor; plain-product by its heavier sub). plain-sort also has a **deterministic** partition check
(no Schwartz–Zippel error). So plain-sort wins at moderate K / total-gates / simplicity.

### F8. plain-product's real justification is the recursion bridge + high-K corner
plain-product earns its place via, in order of durability:
1. **Best recursion inner — O(1) public surface (9 fields, M-independent).** Keeps the
   recursive verifier segment-size-independent: single-segment outer = **704,363 gates
   flat at N=8 *and* N=480**. An plain-sort inner exposes M+4 fields → outer grows with N.
2. **High-K memory:** O(K) glue removes plain-sort's O(N) memory floor; plain-product keeps falling at
   K≥16 where plain-sort plateaus.
3. **Distributed partition check:** O(M)/segment parallel + O(K) merge vs plain-sort's serial
   O(N) sort.
4. **O(K) standalone verifier surface** (washes out under the commitment fix).

So plain-product is *the design you recurse on*, not a cheaper plain-sort. If recursion is cut from the
thesis, plain-product's standalone case gets thin.

### F9. Commitment hiding is computational (with the efficient scheme)
- Poseidon/hash commitment → **computational** hiding (cheap, natural in Noir).
- Pedersen-style → **unconditional content** hiding, but costly in-circuit + only
  computational *binding*.
Even perfectly-hiding committed-* is **a notch weaker than recursion/flat**: it still
publishes K commitments (reveals K; hiding rests on the scheme), whereas
recursion/flat make the partition **structurally absent** (witness) — assumption-free.

### F10. "Perfect hiding" of flat/recursion is *structural*, not IT-ZK
It means the **public surface contains no route/partition information**, not that the
SNARK gives information-theoretic zero-knowledge (UltraHonk-ZK is computational/
statistical and is identical across all variants — not a discriminator).

---

## Part 2 — Reframed thesis spine

**Two structural results, complementary:**
- **Dualism (existing, Ch 8):** decomposition gives no algorithmic ZK speedup; its only
  payoff is parallelism / per-prover memory.
- **Binding tax (new):** the cost of binding K independent proofs back into one sound,
  private statement — one artifact, three symptoms (F3) — organizes the whole variant
  family via two decisions (F5).

**Frontier = pick-two triangle at fixed privacy (F4)**, with folding as the missing
corner. Privacy becomes a *held-constant condition* (commitment fix) or a *measured
axis* (privacy ladder), not the thing that makes variants incomparable.

**Progress-line (each step removes exactly one symptom):**
1. **flat-Merkle** — monolithic; perfect hiding, O(1) verifier, no parallelism.
2. **→ plain-sort** *(buy parallelism)* — stitching tax appears: bookkeeping + plaintext partition
   leak. Cheapest total gates, deterministic.
3. **→ plain-product** *(shrink public surface + un-serialize glue)* — P_i + distributed check.
   NOT cheaper overall; a surface/critical-path optimization. Payoff deferred to step 5.
4. **→ committed-sort/plain-product** *(equalize privacy)* — bind on hiding commitments; leak closes;
   only the O(K) verifier symptom remains.
5. **→ recursion** *(O(1) verifier + perfect hiding)* — bind in-circuit; all three
   symptoms collapse at once. Prover pays 704k×K. **Here plain-product's O(1) surface pays off.**
6. **→ folding** *(drop the prover tax)* — defer verification; the open corner. Future.

---

## Part 3 — Per-implementation privacy classification

Common to all: route hidden by SNARK-ZK; ZK only hides beyond what the statement
implies.

| Implementation | Cost matrix | Partition | Partition privacy class |
|---|---|---|---|
| flat_full (4 perm variants) | disclosed (public) | N/A | — |
| flat_merkle_presence | committed (root) | N/A | — |
| **plain-sort** | committed | node-sets + endpoints plaintext | **disclosed** (unconditional leak) |
| **plain-product** | committed | endpoints plaintext; membership = unblinded `P_i` | **computational, oracle** (~C(N,M), confirms guesses) |
| **committed-sort** (impl., Poseidon) | committed | blinded commitment `C_i` (sort partition in glue) | **computational** (reveals K; on Poseidon) |
| **committed-product** (impl., Poseidon) | committed | blinded commitment `C_i` (grand-product partition) | **computational** (reveals K; on Poseidon) |
| committed-sort/plain-product (Pedersen, analytical) | committed | blinded commitment | **unconditional content** (K revealed, comp. binding) |
| **Recursion** (recursive-sort & recursive-product) | committed | absent (witness) | **structural / assumption-free** |
| Variant B (unimpl.) | sub-matrices disclosed | partition + sub-matrices plaintext | **disclosed** (most) |
| Folding (future) | committed | absent (witness) | **structural / assumption-free** |

**Privacy ladder (assumption-decreasing):**
B → plain-sort → plain-product → committed-sort / committed-product (Poseidon) → committed (Pedersen) →
recursion/folding/flat. Two distinct mechanisms: *commit to hide it* (the committed
rungs) vs *don't put it there at all* (recursion/flat, assumption-free). **Implemented
finding:** committed-sort and committed-product land at the *same* rung (multiset
computational, interior order info-theoretic, reveal K) — they differ only in glue
cost/mechanism (O(N) sort + O(N) commit-fold vs distributed grand-product + O(K)
commit-fold), the equal-privacy restatement of F7. recursive-sort and recursive-product also
have **identical** final privacy.

---

## Part 4 — Decisions (resolved 2026-05-31)

| # | Choice | Resolution |
|---|---|---|
| C1 | Adopt "stitching tax" as 2nd structural result? | **Yes** — it makes the progress-line monotone |
| C2 | committed-sort/plain-product status | **Implemented as full variants** (both committed-sort and committed-product), kept *alongside* plain-sort/plain-product as new frontier points |
| C3 | Commitment scheme | **Poseidon implemented**; Pedersen kept as an analytical unconditional-content upgrade |
| C4 | Privacy treatment in comparison | **Equalize via commitment (code)** — committed-* give the equal-privacy slice; privacy-bits also quantified (§14.4/14.5) |
| C5 | Recursion's status in the narrative | **First-class** — implemented, the perfect-hiding endpoint |
| C6 | Role of plain-sort / plain-product given committed-* | **Keep, reframed** as the *diagnosis* (stitching tax + confirmation-oracle leak) and the *disclosure-regime* points; committed-* are the *cure*. Presented as one progression line, not co-equal variants |

---

## Part 5 — Next steps (ordered, mapped to artifacts)

**Immediate (unblocks the comparison):**
1. ✅ **Privacy-bits quantification** written: per-implementation table + ladder +
   commitment taxonomy now in `HIERARCHICAL_EXPLAINED.md` §9b (design) and §14.4/14.5
   (privacy class + assumption-decreasing ladder).
2. **Frontier figure redesign** (Ch 10): replace the 2D privacy-cost plot with the
   pick-two triangle at fixed privacy; add **recursion as the perfect-hiding endpoint**;
   add an **equal-privacy comparison panel** (flat / committed-sort / committed-product /
   recursion — all at the "partition hidden" slice, differing in cost / parallelism /
   verifier), with plain-sort / plain-product drawn as the upstream *disclosure / oracle-leak* points on the
   same progression line (arrows, not co-equal markers). Privacy-bits annotated. Needs the
   `results/hier_c.csv` / `results/hier_cfs.csv` sweeps for the cost coordinates.

**Outline edits (`Thesis_Outline.md`):**
3. **Ch 8:** add closing section introducing the **stitching tax** (F3) + the two binding
   decisions (F5). Frame: dualism = "why decompose"; stitching tax = "how you pay."
4. **Ch 9:** reframe from "three variants as statements" → "**stitching-tax design space;
   variants as points**." Promote recursion to first-class. Rewrite plain-product's section
   around F8 (the bridge, not a cheaper plain-sort). Add committed-sort/plain-product per C2.
5. **Ch 12:** sharpen folding as *the* corner that breaks the triangle (F4).
6. **Title subtitle:** consider "*…a Structural Dualism, and the Binding Tax of
   Decomposed Proofs*."

**Verifier hardening (if non-recursive variants stay prominent):**
7. Spec / auto-generate the cross-check code from the circuit public-input ABI so it
   can't drift; add property tests. Addresses the "trusted code surface" objection (F2).

**committed-* implementation — DONE (2026-05-31):**
8. ✅ **committed-product** built: `circuits/hierarchical_segment_cfs` + `hierarchical_glue_cfs`,
   blinded `C_i` over `[P_i, h_in, h_out, start, end, partial_cost]`; sub G7 dropped; glue
   G0 recompute. Public `{root, X, C_i}` / `{root, T, X, C_is}`.
9. ✅ **committed-sort** built: `circuits/hierarchical_segment_c` + `hierarchical_glue_c`,
   blinded `C_i` over `[cycle_segment…, partial_cost]`; per-segment sort dropped; glue G0 +
   O(N) sort. Public `{root, C_i}` / `{root, T, C_is}`.
10. ✅ Builder modes `--hierarchical-c` / `--hierarchical-cfs`, verifiers
    `verify_hier_{c,cfs}.py`, harnesses `run_hier_{c,cfs}.py` (CSV `hier_c` / `hier_cfs`),
    correctness suites `test_hierarchical_{c,cfs}.py` (7/7 each), `aggregate_hier.py`
    variant-agnostic. **Pending:** the user runs the `run_hier_{c,cfs}.py` sweeps into
    `results/` and the frontier figure adds the equal-privacy panel.

**Already done (cite, don't redo):** committed-sort/plain-product implemented + documented
(§9b, §14.4/14.5); recursion implemented + benchmarked (plain-product-inner & plain-sort-inner,
`compare_inner.py`); plain-product partition-privacy = computational established in
`HIERARCHICAL_EXPLAINED.md` §9.11/§14.2; recursion inner-choice justified in
`Recursive_inner_circuit_choice_explained.md`.

---

*Sources: `results/{500,hier_a*,hier_fs*,recursion_full,recursion_micro_single}.csv`,
`results/recursion_inner_cmp/`; circuits in `tests/recursion_micro/`,
`circuits/hierarchical_*`; verifiers `pipeline/verify_hier{,_fs}.py`.*
